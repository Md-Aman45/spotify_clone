apiVersion: v1
kind: Service
metadata:
  name: spotify-clone
  labels:
    app: spotify-clone
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: spotify-clone